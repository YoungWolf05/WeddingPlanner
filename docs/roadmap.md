# Wedding Planner Product Roadmap

## Status

| Field | Value |
| --- | --- |
| Last reviewed | 2026-08-06 |
| Current completed milestone | **Phase 9 — Web Interface** |
| Active phase | **None** |
| Next proposed phase | **Phase 10 — Production Hardening and Continuous Evaluation** |

Phase 9 — Web Interface is complete (all four exit criteria met with recorded evidence; increments 9a–9d delivered, reviewed, and tested — 9a versioned SSE v2 contract, 9b server RAG wiring emitting v2 events with Phase-5 auth/ownership/redaction preserved, 9c an isolated React+Vite SPA with a tested zero-credentials-in-bundle guardrail, 9d a deterministic opt-in Playwright browser E2E suite). No phase is active. Phase 10 — Production Hardening and Continuous Evaluation is the next proposed phase and is not active until the user approves its activation.

## Status vocabulary

- **Planned** — recorded future scope that has not been activated; sequencing and details may still require approval.
- **Active** — explicitly activated by the user; implementation or verification is in progress.
- **Complete** — approved exit criteria are met and verification evidence is recorded.
- **Complete (prototype)** — the prototype exit criteria are met; explicitly recorded hardening debt remains and must not be mistaken for production readiness.
- **Blocked** — an active phase cannot proceed; the blocker and required decision or dependency must be recorded.

## Governance

- This document is the source of truth for phase scope and status.
- Activating or completing a phase requires user approval. Intent, partial implementation, or a proposed next phase does not change status.
- Completion requires all approved exit criteria and recorded verification evidence.
- The engineering manager reads this roadmap when scoping work but remains read-only. Roadmap changes are delegated to `feature-implementer` and reviewed by `code-reviewer`.
- Architecture decisions belong in separate architecture decision records (ADRs), not in roadmap scratch notes.

## Roadmap

### Phase 1 — Stateless Chain Foundation

**Status:** Complete

**Goal:** Establish a single-turn wedding-planning assistant through the corporate LiteLLM proxy.

**Key deliverables**

- LCEL prompt → model → string parser chain with the Aria system persona.
- Central model factory and environment-backed LiteLLM configuration.
- Single-message command-line entrypoint.

**Exit criteria**

- [x] A user message can be passed through the stateless chain.
- [x] Local model construction for the chain uses `createChatModel()`.
- [x] The Phase 1 entrypoint is exposed as `npm run chain "<message>"`.

**Implementation evidence and notes**

- `src/core/chain.ts` implements `createWeddingPlannerChain()`; `src/core/model.ts` and `src/config.ts` supply LiteLLM model configuration.
- `src/core/prompts.ts` defines the Aria persona, and `src/run-chain.ts` is mapped to the `chain` script in `package.json`.
- The command identifies the implemented runtime path; it is not an automated regression test and requires live LiteLLM access.

### Phase 2 — Stateful Conversation Prototype

**Status:** Complete (prototype)

**Goal:** Demonstrate multi-turn conversation state keyed by a LangGraph thread identifier.

**Key deliverables**

- LangGraph conversation graph that reuses the Phase 1 model factory and persona.
- Process-local `MemorySaver` checkpointer and `thread_id` session configuration.
- Multi-turn recall demonstration.

**Exit criteria**

- [x] Multiple turns on one `thread_id` share conversation history.
- [x] The graph compiles with a checkpointer and invokes the configured chat model.
- [x] The demonstration is exposed as `npm run memory`.

**Implementation evidence and notes**

- `src/core/chain.ts` implements `createConversationalChain()`; `src/core/memory.ts` provides `MemorySaver` and `sessionConfig()`.
- `src/run-memory.ts` is mapped to the `memory` script in `package.json` and demonstrates recall on one thread.
- State is volatile and process-local. Durable storage, ownership, retention, concurrency, and migration behavior are not implemented; they are Phase 5 scope.
- The demo command is not automated verification and requires live LiteLLM access.

### Phase 3 — Streaming Terminal Product Prototype

**Status:** Complete (prototype)

**Goal:** Provide an interactive terminal experience with streaming responses and basic conversation controls.

**Key deliverables**

- Streaming terminal REPL backed by the Phase 2 graph.
- `/new`, `/model <name>`, and `/exit` controls.
- Per-turn cancellation, recoverable turn errors, and model allow-list handling.

**Exit criteria**

- [x] The terminal accepts repeated user turns and streams model output.
- [x] Users can start a new thread, switch an allowed model, exit, and interrupt an in-flight turn.
- [x] The REPL is exposed as `npm run chat`.

**Implementation evidence and notes**

- `src/cli.ts` implements the REPL, streaming, commands, model switching, and interruption behavior; `package.json` maps it to `npm run chat`.
- Automated REPL coverage does not exist and is explicitly Phase 4 hardening debt. The chat command is a live manual path, not an automated test.

**Completed-phase verification recorded on 2026-07-27**

- Repository source at baseline commit `f2f9678` and `package.json` scripts were inspected against the Phase 1–3 criteria.
- `npm run typecheck` and `npm run build` passed during this roadmap review.
- No live LiteLLM command was run. The repository has no test framework or lint script, so this evidence proves compilation and implementation presence, not automated end-to-end behavior.

### Phase 4 — Engineering Baseline and Provider Contract

**Status:** Complete

**Goal:** Create a dependable engineering baseline and measure the real LiteLLM/provider contract before building provider-dependent features.

**Increment plan**

Phase 4 is delivered as ordered, independently verifiable sub-increments. One sub-increment is worked at a time; each must be independently verifiable before the next begins.

- **4a — Test framework and Phase 1–3 regression coverage.** Status: Complete. Introduce Vitest and deterministic, mocked-model, offline tests covering the Phase 1 chain, Phase 2 thread memory, and Phase 3 REPL controls/streaming/cancellation. Tests must run in CI without live proxy calls. Delivered: Vitest with deterministic offline Phase 1–3 regression tests (32 tests); reviewed.
- **4b — `createChatModel()` centralization.** Status: Complete. Route all chat-model construction through the model factory, including cleanup of the direct `ChatOpenAI` construction in `src/test-connection.ts`. Delivered: `src/test-connection.ts` routed through `createChatModel()`; offline guard test (`test/phase4-model-factory.test.ts`) enforces single-factory `ChatOpenAI` construction; 34 offline tests; `npm run typecheck` and `npm run build` pass; reviewed.
- **4c — LiteLLM capability matrix and embedding assessment.** Status: Complete. Verify the chat-model capability matrix for each supported chat alias and separately assess embedding aliases/endpoints. Exposed as an explicit opt-in live command; not run in default CI. Delivered: opt-in `npm run test:capabilities` live probe plus a dated capability matrix ([`docs/capabilities/2026-07-27.md`](capabilities/2026-07-27.md)); single-factory guards extended so only `src/core/model.ts` constructs `ChatOpenAI` and only `src/core/embeddings.ts` constructs `OpenAIEmbeddings`; host-only secret redaction with fully masked key; 55 offline tests; `npm run typecheck` and `npm run build` pass; reviewed. See "4c findings and open items" below for caveats.
- **4c.1 — Alias reconciliation.** Status: Complete. Remove the invalid `gpt-5.1-chat` alias confirmed rejected by the key/proxy and reconcile the documented alias list. Delivered: `gpt-5.1-chat` removed from the allow-list (`src/core/repl.ts`), `AGENTS.md`, `.env.example`, and the probe alias set, with tests locking its rejection and pinning `ALLOWED_MODELS` to `["claude-opus-4-8", "claude-sonnet-4-6"]`; 57 offline tests; `npm run typecheck` and `npm run build` pass; reviewed.
- **4d — Local tracing with redaction.** Status: Complete. Add local, self-contained tracing with secret/PII redaction and documented observability boundaries. No external SaaS tracing backend. Delivered: opt-in local tracing integrated at the `createChatModel()` boundary via a LangChain callback handler (off by default; enabled with `LITELLM_TRACE=1`, `TRACE=1` alias, `LITELLM_TRACE` authoritative including `=0` disabling even when `TRACE=1`); metadata-only by default (timestamp, model alias, operation, latency, usage when present, outcome, streaming flag, redacted error reason) with optional content capture off by default and redacted when enabled; pluggable sinks (default JSONL to gitignored `logs/`, optional stderr console sink) whose failures are swallowed and never crash a run; always-on, non-disableable redaction scrubs the LiteLLM `apiKey`/`baseURL` plus basic PII (email/phone) with cap-after-scrub so truncation cannot leak a secret; shared redaction extracted to `src/core/redaction.ts` and reused by the probe (which now additionally redacts PII and redacts its top-level stderr path). Trace output goes to gitignored `logs/` only — no secrets/PII, local only. 110 offline tests; `npm run typecheck` and `npm run build` pass; reviewed (Ready, no blocking/recommended findings).
- **4e — Evaluation dataset, documentation alignment, and closeout.** Status: Complete. Add a small versioned wedding-planning evaluation dataset (~10–15 prompts) with a mostly deterministic baseline, align roadmap/developer/environment/operational documentation (including `AGENTS.md` architecture/tracing alignment), and complete Phase 4 closeout. Delivered: a temperature-omit factory option (`createChatModel({ temperature: null })`); a definitive re-probe resolving `claude-opus-4-8` structured output to `Supported` when temperature is omitted (previously undetermined because the factory-injected temperature tripped a model deprecation on the structured path); `gemini-embedding-001` embeddings verified `Supported` at 768 dimensions ([`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md)); a versioned eval dataset at `evals/dataset.jsonl` with pure offline property scorers (`src/core/eval.ts`); an opt-in `npm run eval` live runner and a dated baseline ([`docs/eval/2026-07-28.md`](eval/2026-07-28.md), claude-sonnet-4-6, 12/12 items, 100%); documentation alignment and evidence signposts; 149 offline tests; `npm run typecheck` and `npm run build` pass; reviewed.

**Approved decisions (rationale for future readers)**

- **Test framework:** Vitest.
- **Test strategy:** Deterministic, mocked-model, offline unit/regression tests that run in CI without live proxy calls.
- **Capability checks:** Live LiteLLM capability checks are an explicit opt-in command, not part of default CI.
- **Tracing:** Local, self-contained tracing with secret/PII redaction first; no external SaaS backend.
- **Evaluation set:** A small committed dataset (~10–15 prompts) with a mostly deterministic baseline.
- **Delivery:** Incremental, one sub-increment at a time (4a → 4e), each independently verifiable.

**Key deliverables**

- Test framework and automated Phase 1–3 regression coverage, including the streaming REPL.
- Full `createChatModel()` centralization, including cleanup of direct model construction in `src/test-connection.ts`.
- A chat-model capability matrix verified against the real LiteLLM proxy for each supported chat alias: invoke, stream, abort, usage metadata, tool calls, and structured output.
- A separate embedding alias/endpoint assessment through the proxy. Embeddings are evaluated against embedding aliases, not required of chat aliases.
- Explicit `Supported`, `Unsupported`, `Degraded`, or `N/A` results for every assessed capability.
- An embedding-readiness gate: Phase 7 and Phase 8 cannot activate until an approved embedding alias and endpoint have a dated `Supported` verification.
- Tracing with secret/PII redaction and documented observability boundaries.
- A small, versioned wedding-planning evaluation dataset and repeatable baseline.
- Alignment of roadmap, developer commands, environment guidance, and operational documentation.

**Exit criteria**

- [x] Tests deterministically cover the Phase 1 chain, Phase 2 thread memory, and Phase 3 REPL controls/streaming/cancellation without requiring uncontrolled live calls. (4a; offline Vitest suite, 149 tests.)
- [x] All chat model clients, including connectivity checks, are created through the model factory. (4b; enforced by the single-factory guard test `test/phase4-model-factory.test.ts`.)
- [x] The chat-model matrix records dated `Supported`, `Unsupported`, `Degraded`, or `N/A` results for every supported chat alias and assessed chat capability. (4c/4e; [`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md) records dated states for `claude-opus-4-8` and `claude-sonnet-4-6` — note `claude-opus-4-8` structured output is `Supported` only when temperature is omitted.)
- [x] The separate embedding assessment records the proxy endpoint, candidate embedding aliases, and a dated state for each assessment. (4e; `gemini-embedding-001` = `Supported` at 768 dimensions in [`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md) — the embedding-readiness gate is now MET.)
- [x] Traces demonstrate useful diagnostics without exposing credentials or designated sensitive data. (4d; opt-in local tracing at the `createChatModel()` boundary with always-on secret/PII redaction, JSONL/console sinks, off by default.)
- [x] The evaluation baseline runs repeatably and documentation matches actual commands and behavior. (4e; `npm run eval` over `evals/dataset.jsonl` with a dated baseline at [`docs/eval/2026-07-28.md`](eval/2026-07-28.md).)

**Implementation evidence and notes**

- Model construction is centralized (4b): `src/config.ts` and `src/core/model.ts` centralize application model setup, and `src/test-connection.ts` now constructs its model via `createChatModel()`; an offline guard test (`test/phase4-model-factory.test.ts`) enforces single-factory `ChatOpenAI` construction.
- `src/cli.ts` supports model switching, but this is not a verified provider capability contract.
- Chat-model and embedding assessments are separate contracts. Chat aliases are not required or evaluated as embedding aliases; embedding results attach only to candidate embedding alias/endpoint pairs.
- Vitest is in place with deterministic offline Phase 1–3 regression coverage (4a); the full suite is 110 offline tests. `npm run test:connection` remains the only automated runtime connectivity smoke check and makes a live request.
- Local tracing (4d) is delivered: `src/core/tracing.ts` wires an opt-in LangChain callback handler at the `createChatModel()` boundary (off by default; `LITELLM_TRACE` authoritative, `TRACE` alias), emitting metadata-only records by default with optional redacted content capture, to pluggable sinks (default JSONL to gitignored `logs/`, optional stderr) whose failures are swallowed. Redaction is shared in `src/core/redaction.ts` (always-on secret + PII scrubbing with cap-after-scrub) and reused by `src/probe-capabilities.ts`. Trace output stays local in gitignored `logs/` and carries no secrets/PII.
- Capability assessment (4c) is delivered: `src/core/capabilities.ts` provides pure capability rendering and `classifyAbortOutcome` logic, `src/core/embeddings.ts` is the embeddings factory, and `src/probe-capabilities.ts` backs the opt-in `npm run test:capabilities` live probe (not in `npm test`/CI). Offline coverage adds capability rendering and abort-classification tests. The architecture guard is extended so only `src/core/model.ts` constructs `ChatOpenAI` and only `src/core/embeddings.ts` constructs `OpenAIEmbeddings`. A dated matrix is recorded at [`docs/capabilities/2026-07-27.md`](capabilities/2026-07-27.md).

**4c findings and open items (resolved at Phase 4 closeout)**

- **`claude-opus-4-8` structured output — RESOLVED (`Supported` with temperature omitted).** A definitive re-probe via a temperature-omitting path confirmed structured output is `Supported` for `claude-opus-4-8` (dated evidence: [`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md)). The earlier `Error` was caused by the factory-injected `temperature` tripping a model deprecation on the structured path, not a feature rejection. `claude-sonnet-4-6` remains fully `Supported` across invoke, streaming, abort, usage, tool-calling, and structured output. **Carry-forward constraint for Phase 6:** application structured-output calls to `claude-opus-4-8` must omit temperature (construct the model via `createChatModel({ temperature: null })`) or the model errors on its structured-output path.
- **`gpt-5.1-chat` reconciliation — RESOLVED (4c.1).** The alias was confirmed invalid on this key/proxy ("Invalid model name") and removed from the allow-list (`src/core/repl.ts`), documentation (`AGENTS.md`), `.env.example`, and the probe alias set, with tests locking its rejection (`isAllowedModel("gpt-5.1-chat") === false`; `ALLOWED_MODELS` is exactly `["claude-opus-4-8", "claude-sonnet-4-6"]`). The historical matrix at [`docs/capabilities/2026-07-27.md`](capabilities/2026-07-27.md) preserves the `Error` result from when three aliases were probed.
- **Embedding-readiness gate — RESOLVED / MET.** `gemini-embedding-001` is verified `Supported` at 768 dimensions (dated evidence: [`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md)). The gate no longer blocks Phase 7 or Phase 8 activation. For those phases, `LITELLM_EMBED_MODEL` should be set to a verified embedding alias.

### Phase 5 — Durable Conversation Service

**Status:** Complete

**Goal:** Move volatile conversation state behind a secure, durable service boundary.

**Increment plan**

Phase 5 is delivered as ordered, independently verifiable sub-increments. One sub-increment is worked at a time; each must be independently verifiable before the next begins.

- **5a — Durable SQLite checkpointer.** Status: Complete. Replace the process-local `MemorySaver` with a durable SQLite checkpointer behind the existing checkpointer interface so conversation state survives a process restart. Structured so the checkpointer is swappable to PostgreSQL later without changing callers. Deterministic offline tests use temp SQLite files/temp dirs and a mocked model. Delivered: durable SQLite LangGraph checkpointer (`@langchain/langgraph-checkpoint-sqlite`) behind the existing `BaseCheckpointSaver` interface, replacing the in-RAM `MemorySaver` so conversation state survives a process restart; `src/core/memory.ts` provides `createCheckpointer(dbPath?)`, `resolveCheckpointDbPath(dbPath?)`, and a lazy memoized `getCheckpointer()` (import has no filesystem side effect), with default DB `./data/checkpoints.sqlite` configurable via `CHECKPOINT_DB_PATH` (kept out of the `LITELLM_` namespace) and parent dir auto-created; `createConversationalChain(options, saver?)` is backward-compatible with an optional injected saver and `sessionConfig`/`thread_id` are unchanged; interface remains swappable to PostgreSQL later without changing callers; 154 offline tests (added restart-survival, cross-restart thread isolation, factory/path-resolution, repo-cleanliness guard); `npm run typecheck` and `npm run build` pass; reviewed (Ready for merge, no blocking/recommended findings).
- **5b — Thread identity & ownership model.** Status: Complete. Server-issued UUID thread identifiers, an ownership store, and access checks enforcing thread ownership; list/get/delete operations scoped to the owner; retention/deletion policy. `thread_id` remains a conversation key and is never treated as identity. Delivered: an offline thread ownership model in `src/core/threads.ts` (a `ThreadStore` interface + `Thread` record; `createThreadStore(saver)`) using the shared better-sqlite3 connection from the checkpointer (`getSqliteSaver()` in `src/core/memory.ts` exposes the shared saver — the one intentional concrete-type site for the future Postgres swap); a dedicated `threads` table (server-issued UUID PK via `crypto.randomUUID`, `owner_id`, `title`, `created_at`, `updated_at`, index on `owner_id`) in the SAME SQLite DB; owner-scoped `createThread`/`listThreads`/`getThread`/`touchThread`/`deleteThread` with ownership enforcement and no existence leak (not-owned == not-found); an ATOMIC HARD delete removing the ownership row AND the checkpoint state (checkpoints+writes) for that `thread_id` in one better-sqlite3 transaction; the security-critical `info.changes` ownership gate is compile-checked via type-only `@types/better-sqlite3` (dev-only; no runtime import); library-schema coupling (mirrored checkpoint DDL/DELETEs, pinned to `@langchain/langgraph-checkpoint-sqlite` 1.0.3) is guarded by a version-pin test + a schema-introspection sanity test; no HTTP/SSE/auth transport (that is 5c); 169 offline tests (added cross-owner denial, atomic hard delete removing checkpoints, atomic rollback, restart persistence, version/schema guards); `npm run typecheck` and `npm run build` pass; reviewed (Ready, no blocking/recommended findings).
- **5c — Authenticated HTTP service + SSE streaming.** Status: Complete. A thin custom Node HTTP API for thread create/list/get/delete plus a streaming chat endpoint, bearer-token/user-id auth middleware, a versioned typed Server-Sent Events (SSE) event contract independent of the terminal tuple format, and per-turn cancellation. Delivered: a thin authenticated HTTP service on Node's built-in `node:http` (no new HTTP framework dependency) enforcing the 5b ownership model — `src/core/auth.ts` (bearer-token → `userId` `TokenAuthenticator` with constant-time compare via `timingSafeEqual`, generic 401 for missing/malformed/unknown, `parseAuthTokens` supporting a JSON map or `token:userId` CSV); `src/core/sse.ts` (versioned typed SSE contract, `SSE_PROTOCOL_VERSION=1`, `init`/`token`/`done`/`error` events, `text/event-stream` headers, `SseWriter` guarded against write-after-close); `src/core/server.ts` (`node:http` router, auth-before-dispatch, `POST`/`GET /threads`, `GET`/`DELETE /threads/:id`, `POST /threads/:id/chat` SSE, with the authenticated `userId` as the ONLY `ownerId` source — no client-supplied owner/user id trusted, not-owned == not-found identical 404 with no existence leak, chat 404 BEFORE any stream/checkpoint write, per-turn `AbortController` wired to req/res close for cancellation, all client/log errors redacted via `src/core/redaction.ts`, 64KiB body cap → 413, malformed JSON → 400, empty message → 400, and an injectable `createServer(deps)` for offline testing); `src/run-server.ts` (live `npm run serve` entrypoint binding `SERVICE_PORT` (default 3000) on 127.0.0.1, sharing one `getSqliteSaver()` across store + per-turn `createConversationalChain({ streaming: true }, saver)`, graceful shutdown, NOT imported by tests); `src/config.ts` adds `servicePort` + optional `authTokensRaw`, `.env.example` documents `SERVICE_PORT` + `AUTH_TOKENS` with obviously-fake placeholder tokens, and `package.json` adds the `serve` script. 191 offline tests (added auth 401 parity, cross-user 404 parity, spoof-resistance, SSE happy path + persistence, chat-404-before-stream, cancellation, validation, redaction, best-effort touch, entrypoint-not-imported guard); `npm run typecheck` and `npm run build` pass; no repo db artifacts; 4b/4c guards pass; security-reviewed (Ready, no blocking findings; Recommended item R2 fixed and re-reviewed Ready).
- **5d — Hardening & docs.** Status: Complete. Concurrency behavior, restart/durability validation at the HTTP layer, retention/deletion policy, README/AGENTS/roadmap alignment. Delivered: HTTP `headersTimeout` (10s) + `requestTimeout` (30s) on the `http.Server` (configurable via `SERVICE_HEADERS_TIMEOUT_MS`/`SERVICE_REQUEST_TIMEOUT_MS`) plus a per-turn SSE idle timeout (`SERVICE_SSE_IDLE_TIMEOUT_MS`, default 60000) that aborts the in-flight turn via the `AbortController`, emits a single redacted `error` event, and ends the stream cleanly (resets per token, no re-arm after firing, no double done/error); the timeout parser rejects `0`/negatives loudly; a top-level prototype-pollution guard rejects `__proto__`/`constructor`/`prototype` body keys with 400; the 413 oversized-body path is retained and now bounded by `requestTimeout`; all timeout values are injectable via `deps.timeouts` so tests drive the timeout paths deterministically. Concurrency is validated and documented (single synchronous better-sqlite3 connection on one event loop serializes writes; multi-instance/horizontal concurrency is explicitly out of scope, a Phase 10 concern); end-to-end restart durability is proven through the HTTP service; the retention hook `ThreadStore.pruneThreads({ olderThanEpochMs, ownerId? })` atomically hard-deletes expired threads + their checkpoint state and is owner-scopeable. Docs (`AGENTS.md`, `README.md`, `.env.example`) aligned. 214 offline tests; `npm run typecheck` and `npm run build` pass; reviewed (Ready, no blocking findings; nitpicks N-1/N-2 fixed and re-reviewed Ready). **5d hardening backlog (deferred code-review items carried from 5c) — all delivered in 5d:**
  - **R1 — HTTP server timeouts. DELIVERED (5d).** `headersTimeout`/`requestTimeout` on the server plus a per-turn SSE idle timeout that aborts the stalled turn with a redacted `error` event and clean end; mitigates slow-client/slowloris and long-lived idle SSE connections; all values injectable/configurable.
  - **R3 — prototype-pollution hardening. DELIVERED (5d).** The JSON body boundary rejects top-level `__proto__`/`constructor`/`prototype` keys with 400 (defense-in-depth; the service already reads named fields only and never merges).
  - **R4 — 413 drain-to-EOF trade-off. DELIVERED (5d).** The oversized-body 413 path is retained and now time-bounded by `requestTimeout`, closing the previously unbounded drain concern.
- **5e — Schema versioning & migration.** Status: Complete. Add an app-owned schema version marker plus a forward-only migration runner for the application's own tables (the `threads` table and any future app tables), replacing the bare `CREATE TABLE IF NOT EXISTS`. Shared-DB-safe: the runner must not collide with or attempt to migrate the LangGraph-owned checkpoint tables, which remain the library's responsibility and stay guarded by the version-pin test. Forward-only safety: fail loudly if the DB was written by a newer app schema version than the running code understands. Migrations are atomic and data-preserving; deterministic offline tests cover fresh-init, forward migration, and the newer-than-supported refusal. Rationale: to genuinely satisfy Phase 5 exit criterion (3)'s "schema migration behavior" clause instead of deferring it. Delivered: app-owned schema versioning + forward-only migration runner in `src/core/migrations.ts` (`runMigrations`/`APP_MIGRATIONS`/`getAppSchemaVersion`), run by `createThreadStore` at construction; a dedicated, collision-proof `app_schema_migrations` marker table (append-only, one row per applied migration; not `PRAGMA user_version`) on the shared DB; each step atomic in its own transaction (throwing `up()` rolls back and does not advance the version); additive/data-preserving; idempotent; forward-only fail-loud when the DB is newer than the code's latest known migration; app-vs-library schema boundary preserved (runner never touches the LangGraph-owned `checkpoints`/`writes` tables, guarded by a coexistence test asserting `user_version` stays 0); legacy 5a–5d DB upgrade proven; injectable migration lists; 225 offline tests (`test/phase5-schema-migrations.test.ts`); `npm run typecheck` and `npm run build` pass; reviewed.

**Approved decisions (rationale for future readers)**

- **Persistence backend:** SQLite checkpointer now — durable across restarts with no additional infrastructure — structured so the checkpointer is swappable to PostgreSQL later without changing callers.
- **Service boundary:** A thin custom Node HTTP API in this repository (not the LangGraph Agent Server), keeping everything local with full control over auth.
- **Auth / thread ownership:** A simple authenticated bearer-token / user-id scheme; the service enforces thread ownership. `thread_id` is a conversation key, never identity. Full SSO/OIDC is out of scope for this local baseline, but the ownership model is built correctly.
- **Streaming / API contract:** Server-Sent Events (SSE) with a versioned, typed event contract that is independent of the terminal tuple format.
- **Scope:** Phase 5 is the durable conversation service only (persistence, thread create/list/get/delete with ownership, resume-after-restart, authenticated streaming chat endpoint, retention/deletion, tests). The web UI stays Phase 9; tools/RAG stay in their later phases.
- **Testing:** Service/persistence/ownership/restart tests are deterministic and OFFLINE (mocked model, temp SQLite files/temp dirs) and part of `npm test`; any real end-to-end run stays opt-in/manual.

**Key deliverables**

- Persistent LangGraph checkpointer with migration and operational strategy.
- Server-issued UUID thread identifiers, authenticated ownership/access enforcement, and retention/deletion behavior.
- An API or Agent Server boundary with a versioned, typed event stream.
- Restart, concurrency, isolation, deletion, and migration validation.

**Exit criteria**

- [x] Authorized users can resume their own threads after process restart, while cross-user access is denied. (5a durability behind `BaseCheckpointSaver` + 5d end-to-end restart durability through the HTTP service; store-level cross-owner denial in 5b — not-owned == not-found, no existence leak.)
- [x] Thread creation uses server-issued UUIDs; authentication context is not derived from or conflated with `thread_id`. (5b server-issued `crypto.randomUUID` thread IDs; the `ownerId` for every operation comes ONLY from the authenticated bearer token in 5c, never from any client-supplied field.)
- [x] Retention/deletion and schema migration behavior are implemented and tested. Retention/deletion is implemented and tested in 5d via the atomic, owner-scopeable `ThreadStore.pruneThreads` hook (a callable policy hook, not a scheduler); the schema-migration clause is now satisfied by 5e — app-owned versioning + forward-only migration runner in `src/core/migrations.ts` with a dedicated `app_schema_migrations` marker, covered by migration tests including the legacy 5a–5d DB upgrade. BOTH halves are met.
- [x] Concurrent turns and service restarts preserve documented consistency guarantees. (5d concurrency + restart tests; the single synchronous better-sqlite3 connection on one event loop serializes writes — multi-instance/horizontal concurrency is explicitly out of scope, a future Postgres/Phase 10 concern.)
- [x] The typed service and event contracts are versioned and validated independently of a UI. (5c/5d versioned SSE `init(version)`/`token`/`done`/`error` contract, `SSE_PROTOCOL_VERSION=1`, validated over HTTP `fetch` in offline tests with no UI.)

**Implementation evidence and notes**

- The process-local `MemorySaver` in `src/core/memory.ts` did not satisfy this phase; 5a replaced it with a durable SQLite checkpointer behind the existing `BaseCheckpointSaver` interface (`createCheckpointer`/`getCheckpointer`, `CHECKPOINT_DB_PATH` config), so conversation state now survives a process restart.
- The stale comment in `src/core/memory.ts` that attributed the persistent-saver swap to "Phase 8" has been corrected to Phase 5 as part of 5a (source changed under 5a, not by this roadmap edit).
- 5d hardening delivered HTTP `headersTimeout`/`requestTimeout` + a per-turn SSE idle timeout (abort + single redacted `error` event, clean end, injectable via `deps.timeouts`), a top-level prototype-pollution 400 guard, a `requestTimeout`-bounded 413 path, validated+documented single-connection concurrency, end-to-end restart durability through the service, and the atomic owner-scopeable `ThreadStore.pruneThreads` retention hook; 214 offline tests, `npm run typecheck` and `npm run build` pass.
- Exit criterion (3) is now fully satisfied: its retention/deletion clause by 5d (atomic owner-scopeable `ThreadStore.pruneThreads`) and its schema-migration clause — previously deferred — RESOLVED by 5e. 5e replaces the bare `CREATE TABLE IF NOT EXISTS` with an app-owned schema version marker + forward-only migration runner (`src/core/migrations.ts`, dedicated `app_schema_migrations` table, shared-DB-safe, forward-only-fail-loud, atomic/data-preserving), while the LangGraph checkpoint tables remain the library's responsibility (lazy setup pinned to `@langchain/langgraph-checkpoint-sqlite` 1.0.3, guarded by the version-pin + coexistence tests). 225 offline tests; `npm run typecheck` and `npm run build` pass.

### Phase 6 — Structured Domain Data and Safe Tools

**Status:** Complete

**Goal:** Add validated wedding-domain data and deterministic tools with explicit safety boundaries.

**Increment plan**

Phase 6 is delivered as ordered, independently verifiable sub-increments. One sub-increment is worked at a time; each must be independently verifiable before the next begins.

- **6a — Domain schemas + structured output.** Status: Complete. Define Zod domain artifacts (e.g., `BudgetPlan`, `WeddingTimeline`) and wire `withStructuredOutput()` plumbing (provider-native where the Phase 4 capability matrix confirms support), honoring the Phase 4 carry-forward constraint that `claude-opus-4-8` structured output requires omitting temperature (construct the model via `createChatModel({ temperature: null })`); the default structured-output model is `claude-sonnet-4-6` (fully `Supported` per the [2026-07-28 capability matrix](capabilities/2026-07-28.md)). Deterministic offline tests validate the schemas and the structured-output path with a mocked model. Delivered: typed Zod wedding-domain schemas in `src/core/schemas.ts` with a PLAIN LLM-facing budget schema (`budgetCategorySchema`/`budgetPlanSchema`, per-field constraints only — total>0, amount>=0, optional percentage 0..100, nonempty names, min-1 categories; plus `planningChecklistSchema`/`checklistItemSchema` and inferred types), keeping the cross-field over-allocation BUSINESS RULE SEPARATE from generation via a pure exported `validateBudgetAllocation(plan)` and a strict `budgetPlanStrictSchema` (`.refine(sum<=total*1.001)`) intended for the 6b tool/domain layer, so the generation schema converts cleanly to JSON Schema (no ZodEffects drop) and won't spuriously reject valid over-allocated first-pass budgets; a structured-output helper `src/core/structured.ts` (`generateStructured<T>`, `generateBudgetPlan`/`generatePlanningChecklist`) honoring the opus temperature-omit rule via a pure `decideStructuredModelOptions(model)` (opus → `{temperature:null}`, sonnet/other → `{}`), building the model via `createChatModel` (4b guard — no direct `ChatOpenAI`), with defense-in-depth `safeParse` re-validation of `withStructuredOutput`, an empty/whitespace-input fast-fail before model construction, and concise redacted validation errors distinct from transport errors; `zod` added as a direct dependency (`^4.4.3`, single resolved copy, no dual-package hazard); not yet wired into tools/agent/HTTP/CLI (6b/6c); 262 offline tests; `npm run typecheck` and `npm run build` pass; 4b/4c guards pass; reviewed (Ready; two Recommended items and two nitpicks fixed and re-reviewed Ready).
- **6b — Safe tools.** Status: Complete. Implement `days_until(date)` and `split_budget(total, categories?)` as pure, deterministic, read-only functions (no filesystem/network/writes) plus `tool()` wrappers with Zod input schemas; unit-test the pure functions directly (boundary/validation cases). No side-effecting/write tools in Phase 6. The 6a over-allocation strict rule (`validateBudgetAllocation` / `budgetPlanStrictSchema`) is the intended consumer of the 6b budget tool's domain layer. Delivered: two SAFE, pure, deterministic, read-only tools in `src/core/tools.ts` with `tool()` wrappers (names `days_until` / `split_budget`, descriptions, Zod input schemas, `content_and_artifact` result shape) — `daysUntil(targetDate, now?)` is UTC calendar-day only (time-of-day/DST independent), strict `YYYY-MM-DD` parsing that rejects impossible/malformed dates, injectable `now` for determinism, returning normalized `targetDate` + signed `daysUntil` + `direction` (future/past/today); `splitBudget(total, categories?, currency?)` reuses the 6a `budgetPlanSchema`/`validateBudgetAllocation`/`budgetPlanStrictSchema`/`BUDGET_ALLOCATION_TOLERANCE` (not duplicated) with a default wedding split summing to 100%, custom percentages OR amounts (mixed/both rejected), largest-remainder (Hamilton) rounding so integer category amounts sum EXACTLY to the target, over-allocation rejected via the 6a domain rule while under-allocation is allowed, currency set only when provided; an exported `weddingTools = [daysUntilTool, splitBudgetTool]` registry for 6c; no agent/tool-loop, HTTP/CLI, or model construction (4b/4c guards intact); `src/core/schemas.ts` unmodified (reused). 306 offline tests (`test/phase6-tools.test.ts`); `npm run typecheck` and `npm run build` pass; reviewed (Ready; one Blocking finding B1 fixed and re-reviewed Ready, plus R1/R2/N2).
- **6c — Tool-loop agent.** Status: Complete. Wire a LangChain tool-calling path so the bot actually calls the 6b `weddingTools`, keeping the existing conversational graph for plain chat (no hand-wired `bindTools` in the current graph). End-to-end exit-criterion test ("how many days until 2026-12-12 and split a $30k budget" exercising both `days_until` and `split_budget`) runs offline against a mocked model emitting tool calls. Delivered: a wedding-planning TOOL-LOOP AGENT in `src/core/agent.ts` — `createWeddingAgent(options?)` wires the 6b `weddingTools` into LangGraph's prebuilt `createReactAgent({ llm, tools, prompt })` (from `@langchain/langgraph/prebuilt`; installed `langgraph` 1.x, NOT the `langchain` meta-package, per the accepted Phase 6 decision) with the Aria persona (`WEDDING_PLANNER_SYSTEM_PROMPT`), building the chat model via `createChatModel` (4b guard) and honoring the opus temperature-omit rule by reusing the shared `isTemperatureOmitModel` predicate from `structured.ts` (DRY); `runWeddingAgent(agent, msg)` returns the final assistant text; `DEFAULT_AGENT_MODEL=claude-sonnet-4-6`; the plain-chat conversational graph (`chain.ts`) is untouched and no HTTP/CLI/service wiring is added (deferred to 6d). The headline exit-criterion is proven OFFLINE with a fixed system clock and a scripted tool-calling fake (`test/helpers/fake-model.ts`) driving the REAL react loop + REAL `ToolNode` + REAL 6b tools: "days until 2026-12-12 + split a $30k budget" ⇒ the agent calls both tools, `days_until` returns the exact `daysUntil` (direction future) and `split_budget` category amounts sum EXACTLY to 30000, and the loop completes with a final message; single-tool, no-tool, and tool-error (invalid date ⇒ graceful `ToolNode` error `ToolMessage`) paths and the opus-temp decision are also covered (`test/phase6-agent.test.ts`, 11 tests). 317 offline tests; `npm run typecheck` and `npm run build` pass; 4b/4c guards pass; no repo db artifacts; reviewed (Ready; no blocking findings; one Recommended R1 + a nitpick addressed as doc-only comments). **Known future-migration item (deliberate deferral, not a defect):** `createReactAgent` is symbol-level `@deprecated` in `langgraph` 1.4.7 (steering toward the `langchain` meta-package's `createAgent`); the prebuilt import is intentionally kept per the accepted Phase 6 no-meta-package decision (documented in-code), and migrating to `createAgent` is a deliberate future decision to consider.
- **6d — Hardening + docs + closeout.** Status: Complete. Add per-tool timeouts and typed/bounded error-handling polish, capability/model-selection notes, `AGENTS.md`/`README.md`/roadmap alignment, and Phase 6 closeout with recorded verification evidence. Delivered: a bounded tool-execution timeout wrapper in `src/core/tool-runtime.ts` (`withToolTimeout`/`invokeToolWithTimeout` + typed, redacted `ToolTimeoutError`; `timeoutMs` injectable/validated — a defensive bound, not an SLA, since the 6b tools are synchronous/instantaneous; deterministic fake-timer timeout tests in `test/phase6-tool-runtime.test.ts`); the three named, redacted refusal/malformed/provider paths in `src/core/structured.ts` hardened (N1: an empty object is treated as refusal only when NOT schema-valid, so a schema-valid `{}` is returned while budget/checklist `{}` remain refusals); only-permitted-execution + tool-state/error coverage at the agent typed message-stream level (`AIMessage.tool_calls` → `ToolMessage` result/`status: "error"`; unknown/unpermitted tool refused by the real `ToolNode` and never executed) in `test/phase6-agent-contract.test.ts`; a pure per-alias contract classifier/renderer in `src/core/contracts.ts` (offline tests in `test/phase6-contract-probe-render.test.ts`) plus an opt-in LIVE per-alias tool-call + structured-output contract probe `src/probe-contracts.ts` (`npm run test:contracts`, opus built with temperature omitted) with dated evidence [`docs/contracts/2026-08-05.md`](contracts/2026-08-05.md) recording `claude-opus-4-8` and `claude-sonnet-4-6` BOTH `Supported` for tool-call AND structured output; `AGENTS.md`/`README.md`/roadmap docs aligned; 357 offline tests; `npm run typecheck` and `npm run build` pass; reviewed. Known future-migration item retained: `createReactAgent` is symbol-level `@deprecated` in `langgraph` 1.4.7; migrating to the `langchain` `createAgent` is a deliberate future decision. Wiring the agent into the CLI/HTTP SSE service is deliberately DEFERRED to a later phase.

**Approved decisions (rationale for future readers)**

- **Structured output:** Zod schemas + `withStructuredOutput()` (provider-native where the Phase 4 capability matrix confirms support); typed domain artifacts (e.g., `BudgetPlan`, `WeddingTimeline`) validated with Zod.
- **Temperature-omit constraint:** Honor the Phase 4 carry-forward — `claude-opus-4-8` structured output requires omitting temperature, so structured-output paths construct the model via `createChatModel({ temperature: null })`; the default structured-output model is `claude-sonnet-4-6` (fully `Supported` per the 2026-07-28 capability matrix).
- **First safe tools:** Deterministic, pure, read-only `days_until(date)` and `split_budget(total, categories?)` — no filesystem/network/writes. A timeline/checklist generator may follow later.
- **Agent vs manual:** Use LangChain `createAgent` for the tool-calling path (only where a real tool loop exists) while keeping the existing conversational graph for plain chat; do not hand-wire `bindTools` into the current graph.
- **Safety boundaries:** Tools are pure/local with validated inputs, per-tool timeouts, and typed error handling; NO side-effecting/write tools in Phase 6 (those require explicit auth/approval in a later phase).
- **Testing:** Deterministic and OFFLINE — tools unit-tested directly; agent/tool-loop and structured-output paths tested with a mocked model emitting tool calls; no live calls in `npm test`; any real end-to-end run stays opt-in.

**Key deliverables**

- Zod schemas for approved domain inputs, outputs, and tool contracts.
- Provider/tool-based structured output selected from Phase 4 capability evidence.
- Deterministic budget and date/timeline tools.
- `createAgent` only when a real model-driven tool loop exists.
- Tool permissions, input validation, timeouts, bounded errors, and auditable execution state.
- Model-specific capability and contract tests.

**Exit criteria**

- [x] Structured outputs and tool inputs are runtime-validated, with safe handling of refusal, malformed data, and provider errors. (6a `src/core/structured.ts` `generateStructured`/`generateBudgetPlan` request provider-native structured output via `withStructuredOutput` then RE-VALIDATE with `safeParse`, distinguishing THREE named, redacted paths — transport error / refusal-or-no-output / schema-validation — hardened in 6d (N1 empty-object rule); tool inputs are runtime-validated by Zod input schemas at the `tool()` boundary in 6b `src/core/tools.ts`; offline tests in `test/phase6-structured.test.ts`.)
- [x] Budget/date tools are deterministic and have boundary, timeout, and failure tests. (6b `src/core/tools.ts` `days_until`/`split_budget` are pure/deterministic with boundary + validation-failure tests in `test/phase6-tools.test.ts`; 6d `src/core/tool-runtime.ts` adds a bounded, injectable tool-execution timeout (`withToolTimeout`/`invokeToolWithTimeout` + typed redacted `ToolTimeoutError`) with deterministic fake-timer timeout tests in `test/phase6-tool-runtime.test.ts` — a defensive bound, not an SLA, since the tools are synchronous/instantaneous.)
- [x] Only permitted tools execute, and tool state/errors are represented in the typed event contract. (6c/6d — satisfied at the AGENT MESSAGE-STREAM level: a tool INTENTION is an `AIMessage.tool_calls` entry, a RESULT is a `ToolMessage` (+artifact, linked by `tool_call_id`), and an ERROR is a `ToolMessage` with `status: "error"`; an unknown/unpermitted tool is refused by the real `ToolNode` and NEVER executes — `test/phase6-agent-contract.test.ts`. **USER-ENDORSED interpretation:** "typed event contract" = the agent's typed LangGraph message stream; wiring the agent into the HTTP SSE event contract (`src/core/sse.ts`) and the CLI is DELIBERATELY DEFERRED to a later phase. This is recorded transparently as an honest, user-approved reading, not an overclaim.)
- [x] Every enabled model alias passes the required tool-call and structured-output contract tests. (6d pure classifier/renderer `src/core/contracts.ts` with offline tests in `test/phase6-contract-probe-render.test.ts`, plus the opt-in LIVE per-alias probe `src/probe-contracts.ts` / `npm run test:contracts` (opus built with temperature omitted); dated evidence [`docs/contracts/2026-08-05.md`](contracts/2026-08-05.md) records `claude-opus-4-8` and `claude-sonnet-4-6` BOTH `Supported` for tool-call AND structured output.)

**Implementation evidence and notes**

- Structured domain output is delivered (6a): `src/core/schemas.ts` defines the typed Zod wedding-domain schemas (plain LLM-facing `budgetPlanSchema`/`planningChecklistSchema`) with the cross-field over-allocation business rule kept OFF the LLM path (`validateBudgetAllocation`/`budgetPlanStrictSchema`); `src/core/structured.ts` (`generateStructured`/`generateBudgetPlan`/`generatePlanningChecklist`) requests provider-native structured output via `withStructuredOutput`, re-validates with `safeParse`, and reports three distinct redacted failure paths (transport / refusal-or-no-output / schema-validation), honoring the opus temperature-omit rule via `decideStructuredModelOptions`/`isTemperatureOmitModel` (sonnet default) and building the model through `createChatModel` (4b guard).
- Safe tools and a bounded timeout are delivered (6b/6d): `src/core/tools.ts` exposes exactly two SAFE, pure, deterministic, read-only tools (`days_until`, `split_budget`) with Zod input schemas and no I/O; `src/core/tool-runtime.ts` adds an injectable, defensive tool-execution timeout with a typed redacted `ToolTimeoutError`.
- A tool-loop agent is delivered (6c): `src/core/agent.ts` (`createWeddingAgent`/`runWeddingAgent`, `DEFAULT_AGENT_MODEL=claude-sonnet-4-6`) wires the 6b `weddingTools` into LangGraph's prebuilt `createReactAgent({ llm, tools, prompt })` with the Aria persona; only the two `weddingTools` are bound and executable, and tool state/errors are represented on the typed LangGraph message stream (`tool_calls` / `ToolMessage` `status`).
- Per-alias contract coverage is delivered (6d): pure `src/core/contracts.ts` classification/rendering with offline tests, plus the opt-in LIVE `npm run test:contracts` probe and dated evidence [`docs/contracts/2026-08-05.md`](contracts/2026-08-05.md) (both enabled aliases `Supported` for tool-call and structured output; opus with temperature omitted).
- Deterministic offline coverage stands at 357 tests; `npm run typecheck` and `npm run build` pass; all increments reviewed.
- **Deliberate deferred work (future scope, not defects):** (a) wiring the agent and its typed tool events into the CLI (`npm run chat`) and the HTTP SSE service (`src/core/sse.ts` / `npm run serve`) is deferred to a later increment/phase — the agent is not yet reachable from those surfaces; (b) `createReactAgent` is symbol-level `@deprecated` in `langgraph` 1.4.7, and migrating to the `langchain` meta-package's `createAgent` is a deliberate future decision (the prebuilt import is intentionally kept per the accepted no-meta-package decision).

### Phase 7 — Knowledge Ingestion and Retrieval

**Status:** Complete

**Goal:** Build a durable, testable retrieval foundation before generating citation-bearing answers.

**Key deliverables**

- Idempotent ingestion with stable document/chunk IDs and trusted metadata.
- Persistent vector store and an embedding alias verified through the LiteLLM proxy.
- Explicit add, update, and delete semantics without duplicate chunks.
- Retrieval-only evaluation dataset and metrics.

**Exit criteria**

- [x] Re-ingesting unchanged content produces no duplicate documents or chunks. (7b idempotent ingestion — content/source-addressed identity short-circuits unchanged content with no embed and no writes, backed by `UNIQUE(document_id, chunk_index)` + transactional writes; `src/core/chunking.ts`, `src/core/ingestion.ts`; offline tests `test/phase7-chunking.test.ts`, `test/phase7-ingestion.test.ts`.)
- [x] Updates and deletes produce deterministic index state while preserving documented identity rules. (7c source-addressed identity (Option C) — `document_id = sha256(normalizeSourceUri(source_uri))`, `content_hash` as the version marker, an upsert-by-source state machine (created/unchanged/updated/skipped) with atomic in-place chunk+vector REPLACE (created_at preserved, updated_at bumped, zero orphan vectors) plus a delete API (`deleteDocument`/`deleteSource`); two different sources with identical content ⇒ two documents, same source with changed content ⇒ in-place update; `src/core/knowledge-store.ts`, `src/core/ingestion.ts`; offline tests `test/phase7-update-delete.test.ts` and updated 7a/7b tests.)
- [x] Metadata can support later authorization and trusted citations without relying on model-generated identifiers. (7e retriever resolves each hit to TRUSTED, APP-OWNED store metadata — chunkId, documentId, sourceUri, chunkIndex, ownerId, contentHash, distance, score — pulled FROM THE STORE, never from model text, with an optional `ownerId` authorization filter as the authz seam; `src/core/retriever.ts`; offline tests `test/phase7-retriever.test.ts`. Demonstrated via retriever trusted-metadata resolution; answer generation and citations-in-text are deferred to Phase 8.)
- [x] The embedding alias and vector dimensions are verified through the proxy and covered by compatibility checks. (7d pure compatibility module `src/core/embedding-compat.ts` — `classifyEmbeddingCompatibility`/`isEmbeddingDimensionCompatible` + renderers — with `config.embedDim` as the single source of truth; offline tests `test/phase7-embedding-compat.test.ts`; opt-in LIVE probe `src/probe-embedding.ts` (`npm run test:embedding`) with dated evidence [`docs/embeddings/2026-08-06.md`](embeddings/2026-08-06.md). **Native-vs-truncated interpretation (user-accepted):** the probe records `gemini-embedding-001` at observed NATIVE 3072 vs expected store dim 768 ⇒ DimensionMismatch, because the probe DELIBERATELY OMITS the `dimensions` request so it observes the alias's native size and can still detect a genuinely wrong-dimension alias. 768 is a deliberate Matryoshka TRUNCATION that the store-writing adapters (`createDocumentEmbedder` in `src/core/ingestion.ts`, `createQueryEmbedder` in `src/core/retriever.ts`) request EXPLICITLY, so live ingestion/retrieval WORK end-to-end at 768 (proven by the 7e retrieval baseline). This native-vs-expected divergence for a truncation-capable alias is a DOCUMENTED, EXPECTED reading — the alias is verified through the proxy and used at the store dimension via explicit truncation — NOT an incompatibility. (Also fixed: `encodingFormat: "float"` root cause so live embeddings return meaningful vectors.))
- [x] Retrieval quality meets an approved baseline before answer-generation tuning begins. (7e retrieval-only eval — pure `recall@k`/`precision@k`/`MRR`/`nDCG@k` + aggregator + `evaluateBaseline` gate in `src/core/retrieval-eval.ts`; offline tests `test/phase7-retrieval-eval.test.ts`; curated benign PII-free corpus `knowledge/corpus/*.md` (provenance `knowledge/README.md`); versioned dataset `evals/retrieval.jsonl`; opt-in LIVE runner `src/run-retrieval-eval.ts` (`npm run eval:retrieval`, ephemeral temp store). **USER-APPROVED baseline thresholds:** recall@k ≥ 0.80, precision@k ≥ 0.18, MRR ≥ 0.70, nDCG@k ≥ 0.75. Dated evidence [`docs/retrieval/2026-08-06.md`](retrieval/2026-08-06.md): recall@5 1.000, precision@5 0.227, MRR 1.000, nDCG@5 1.000 ⇒ PASS. The precision threshold 0.18 sits just below the ~0.227 dataset ceiling (the theoretical max for this mostly-single-relevant dataset) BY CONSTRUCTION — documented, not gate-gaming.)

**Implementation evidence and notes**

- Phase 7 is complete (user-approved) with all five exit criteria met, each implemented → code-reviewed → verified across increments 7a–7e. The deterministic offline suite stands at 498 tests; `npm run typecheck` and `npm run build` pass.
- Persistence & schema (7a): an app-owned knowledge-base SQLite store (its own better-sqlite3 + sqlite-vec file at `KNOWLEDGE_DB_PATH`) with documents + chunks tables, trusted-metadata columns, content-hash stable IDs, and the Phase-5 versioned forward-only migration pattern; `src/core/knowledge-store.ts` (`computeDocumentId`/`computeChunkId`, `searchChunksByVector` KNN).
- Idempotent ingestion (7b): deterministic recursive character chunking (`src/core/chunking.ts`, `chunkText`) + content-hash doc/chunk IDs + an injectable `DocumentEmbedder` seam and idempotent source-addressed upsert (`src/core/ingestion.ts`); unchanged content short-circuits with no embed and no writes.
- Source-addressed identity + update/delete (7c): `document_id = sha256(normalizeSourceUri(source_uri))` with `content_hash` as the version marker; atomic in-place chunk+vector REPLACE (created_at preserved, updated_at bumped, zero orphan vectors) and a `deleteDocument`/`deleteSource` API; identity rules documented and tested (identical content across two sources ⇒ two documents; changed content at one source ⇒ in-place update).
- Embedding/dimension compatibility (7d): pure `src/core/embedding-compat.ts` classifier/predicate/renderers with `config.embedDim` as the single source of truth; offline dimension guards + the opt-in LIVE `npm run test:embedding` probe and dated evidence [`docs/embeddings/2026-08-06.md`](embeddings/2026-08-06.md). The evidence records the NATIVE reading (observed 3072 vs expected 768 ⇒ DimensionMismatch); this is the user-accepted native-vs-truncated interpretation — live ingestion/retrieval run at 768 via EXPLICIT Matryoshka truncation requested by the store-writing adapters, while the probe omits `dimensions` to observe native size and still catch a genuinely wrong-dimension alias. A companion `encodingFormat: "float"` fix made live embeddings return meaningful vectors.
- Retriever + retrieval-only eval + corpus + approved baseline (7e): a PURE `retrieve()` over an injected `QueryEmbedder` + store (`src/core/retriever.ts`) resolving each hit to TRUSTED, APP-OWNED store metadata with an optional `ownerId` authz filter; a PURE retrieval-only eval (`src/core/retrieval-eval.ts`, `recall@k`/`precision@k`/`MRR`/`nDCG@k`, aggregator, `evaluateBaseline`, renderers) over the curated `knowledge/corpus/*.md` and versioned `evals/retrieval.jsonl`; the opt-in LIVE `npm run eval:retrieval` runner (ephemeral temp store outside the repo) with dated evidence [`docs/retrieval/2026-08-06.md`](retrieval/2026-08-06.md).
- **ACCEPTED retrieval baseline thresholds (user-approved at Phase 7 closeout):** recall@k ≥ 0.80, precision@k ≥ 0.18, MRR ≥ 0.70, nDCG@k ≥ 0.75. Met per [`docs/retrieval/2026-08-06.md`](retrieval/2026-08-06.md) (recall@5 1.000, precision@5 0.227, MRR 1.000, nDCG@5 1.000 ⇒ PASS); the precision threshold sits just below the ~0.227 mostly-single-relevant dataset ceiling by construction (documented, not gate-gaming).
- **Deliberate deferred work (future Phase 8 scope, not defects):** grounded answer generation, trusted citations embedded in generated text, and groundedness/citation-correctness/prompt-injection evaluations are Phase 8, not Phase 7. Exit criterion 3 is satisfied here at the retrieval layer via trusted app-owned metadata resolution; wiring that metadata into citation-bearing generated answers is Phase 8.

**Approved design decisions (rationale for future readers)**

- **Vector store:** the sqlite-vec extension on better-sqlite3 (consistent with the existing SQLite stack; single-file durability; transactions with metadata). pgvector remains deferred to Phase 10.
- **Storage location:** a SEPARATE knowledge-base DB file via a new `KNOWLEDGE_DB_PATH` env var; the conversation checkpoints DB (`CHECKPOINT_DB_PATH`) is untouched. The app-owned knowledge schema evolves via the SAME versioned forward-only migration pattern established in Phase 5 (`src/core/migrations.ts` style).
- **Embedding:** reuse `createEmbeddingsModel` (`src/core/embeddings.ts`); the Phase 7 embedding alias is `gemini-embedding-001` at 768 dimensions (embedding-readiness gate already MET, dated evidence [`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md)), configured via `LITELLM_EMBED_MODEL`.
- **Identity:** application-owned, content-hash-based stable document/chunk IDs (never model-generated); deterministic chunking (recursive splitter, fixed size + overlap) so chunk IDs are reproducible.
- **Metadata:** trusted metadata (source URI, document ID, chunk index, content hash, timestamps, and an owner/authorization-ready field) to support later authorization and trusted citations without relying on model-generated identifiers.
- **Testing:** consistent with the recorded cost-aware policy and the OFFLINE/CI-safe suite — offline deterministic tests use an INJECTED fake embedder (no network) to prove idempotency, update/delete, and ID stability; a LIVE, opt-in retrieval eval command produces dated evidence (`docs/retrieval/<date>.md`) for the retrieval-quality baseline and is NEVER part of `npm test`.
- **Scope boundary:** Phase 7 is the retrieval foundation ONLY. No answer generation and no citations (those are Phase 8); no web UI (Phase 9).

**Increment plan (7a–7e)**

Phase 7 is delivered as ordered, independently verifiable sub-increments; each is delivered as implement → code-review → verify. One sub-increment is worked at a time.

- **7a — Persistence & schema foundation.** App-owned knowledge-base SQLite store at `KNOWLEDGE_DB_PATH` with sqlite-vec wiring; documents + chunks tables; trusted metadata columns; content-hash stable ID scheme; versioned forward-only migrations (Phase-5 pattern). (Contributes to exit criterion 3.)
- **7b — Idempotent ingestion.** Deterministic chunking + content-hash doc/chunk IDs + embedding integration (injectable embedder) + idempotent upsert. Targets exit criterion 1 (re-ingesting unchanged content produces no duplicate documents or chunks).
- **7c — Update/delete semantics.** Deterministic index state, documented identity rules, no orphaned or duplicate chunks. Targets exit criterion 2 (and completes exit criterion 3 alongside 7a).
- **7d — Embedding/dimension verification + compatibility checks.** Offline dimension-guard plus an opt-in live verification tied to the capabilities evidence. Targets exit criterion 4.
- **7e — Retriever + retrieval-only eval.** Retriever plus a retrieval-only eval dataset & metrics (recall@k, MRR/nDCG) + approved baseline; includes authoring a small curated, benign, PII-free wedding-domain knowledge corpus under `knowledge/` as the ingestion input and basis for the retrieval-only eval set. Targets exit criterion 5.
- **Exit criterion 3** (metadata supports later authorization and trusted citations without model-generated identifiers) is satisfied across 7a + 7c.

### Phase 8 — Grounded Answers and Trusted Citations

**Status:** Complete

**Goal:** Generate answers grounded in retrieved evidence with citations the application can trust.

**Key deliverables**

- Deterministic two-step retrieval-then-generation RAG as the initial design.
- Trusted citation objects tied by application code to retrieved document/chunk IDs.
- Explicit insufficient-evidence behavior.
- Groundedness, citation correctness, and prompt-injection evaluations.
- Agentic retrieval only after a measured need demonstrates an advantage over two-step RAG.

**Exit criteria**

- [x] Citation objects resolve to retrieved, authorized source IDs and are never accepted solely from model text. (8a APP-ASSIGNED integer citation markers — the model emits only integers; app code owns the marker→chunk/document map — plus 8b `resolveCitations` (`src/core/citations.ts`) that copies every `TrustedCitation` identity field FROM the store-backed `RetrievedChunk`, drops unknown/hallucinated markers (`unknown_marker`) and owner-mismatched hits (`unauthorized`, defense-in-depth), never accepting an ID from model text; `src/core/rag.ts`, `src/core/citations.ts`; offline tests `test/phase8-rag.test.ts`, `test/phase8-citations.test.ts`.)
- [x] Answers distinguish supported claims from insufficient evidence. (8c evidence policy `src/core/evidence.ts` — a pre-generation low-score gate `filterUsableEvidence(score >= minScore; DEFAULT_MIN_EVIDENCE_SCORE=0.5, ratified)` short-circuits to `insufficient` WITHOUT calling the model when no usable evidence remains, and post-generation `reconcileEvidence` yields the app-authoritative `evidenceStatus` `"supported"`/`"insufficient"` — `supported` ONLY if the model claims sufficiency AND ≥1 trusted citation resolves; any ungrounded answer is forced to `insufficient` (structural invariant: `supported` ⟺ ≥1 citation); `src/core/rag.ts`, `src/core/evidence.ts`; offline tests `test/phase8-rag.test.ts`, `test/phase8-evidence.test.ts`.)
- [x] Evaluation covers groundedness, citation precision/recall, malicious source instructions, and missing evidence. (8d pure deterministic scorers + dataset + opt-in live eval — `src/core/rag-eval.ts` (strict `evals/rag.jsonl` parser + groundedness / citation precision-recall-F1 (document-level via the stable `computeDocumentId`) / injection-resistance (no-payload) / missing-evidence scorers + aggregator + `evaluateRagBaseline` + renderers); dataset `evals/rag.jsonl` (16 items: 8 grounded, 4 missing_evidence, 4 injection); benign labeled injection fixture `knowledge/corpus/injection-test-faq.md` (payload marker `PWNED`); opt-in LIVE runner `src/run-rag-eval.ts` (`npm run eval:rag`, ephemeral temp store, dated evidence, never in `npm test`/CI); offline tests `test/phase8-rag-eval.test.ts`. **USER-RATIFIED baseline thresholds:** `meanCitationPrecision ≥ 0.70`, `groundednessPassRate ≥ 0.80`, `meanCitationRecall ≥ 0.80`, `injectionResistanceRate = 1.00`, `missingEvidenceAccuracy ≥ 0.80`. Dated evidence [`docs/rag-eval/2026-08-06.md`](rag-eval/2026-08-06.md): groundedness 1.000, citation precision 0.719 / recall 1.000 / F1 0.800, injection 1.000, missing-evidence 1.000, overall 0.750 ⇒ baseline gate PASS. The precision bar 0.70 is ratified below the ~0.75 observed ceiling BY CONSTRUCTION — a small topically-overlapping corpus + single-relevant-dominant labels + a stochastic model cap realistic precision, while recall/groundedness/injection/missing sit at ceiling; documented FOLLOW-UP: richer multi-source relevance labels and/or a larger, less-overlapping corpus to support a higher precision bar in a later phase.)
- [x] Any move to agentic retrieval is supported by comparative measurements and a separate ADR. (Satisfied by [`docs/adr/0001-defer-agentic-retrieval.md`](adr/0001-defer-agentic-retrieval.md) (Accepted) — it RECORDS the deferral of agentic retrieval; adoption is gated on comparative measurements vs the two-step baseline + a future ADR. NO agentic retrieval is implemented this phase.)

**Implementation evidence and notes**

- Phase 8 is complete (user-approved) with all four exit criteria met, each implemented → code-reviewed → verified across increments 8a–8d. The deterministic offline suite stands at 602 tests; `npm run typecheck` and `npm run build` pass. The deterministic two-step RAG is delivered as a PURE/INJECTED library (offline tests mock the `createChatModel` boundary + inject a fake embedder + a temp sqlite-vec store); it is NOT wired into the HTTP service / CLI / SSE this phase (that is Phase 9).
- Grounded generation core (8a): a pure/injected `answerQuestion` pipeline (`src/core/rag.ts`) — `retrieve()` → a delimited context block where each retrieved chunk carries an APP-ASSIGNED integer citation marker mapped by app code to its chunkId/documentId → structured `GroundedAnswer` generation returning `{ answer, citations, insufficientEvidence }` reusing the Phase 6 structured-output infrastructure (incl. the opus temperature-omit rule); the model emits only integer markers.
- Trusted citation resolution (8b): `src/core/citations.ts` `resolveCitations` maps each model-emitted marker back to the retrieved, AUTHORIZED chunk/document from the 8a context set, copying every identity field FROM the store-backed `RetrievedChunk` (never from model text), dropping unknown/hallucinated markers (`unknown_marker`) and owner-mismatched hits (`unauthorized`, defense-in-depth); this is the crux of exit criterion 1.
- Insufficient-evidence policy (8c): `src/core/evidence.ts` — the pre-generation low-score gate `filterUsableEvidence` (`DEFAULT_MIN_EVIDENCE_SCORE=0.5`, ratified) short-circuits to `insufficient` without calling the model when no usable evidence remains, and post-generation `reconcileEvidence` yields the app-authoritative `evidenceStatus` (`supported` ⟺ ≥1 trusted citation resolves); any ungrounded answer is forced to `insufficient`.
- Evaluations + ratified baseline + ADR (8d): pure deterministic scorers + aggregator + `evaluateRagBaseline` + renderers in `src/core/rag-eval.ts` over the strict `evals/rag.jsonl` dataset (16 items across grounded/missing_evidence/injection) and the benign labeled injection fixture `knowledge/corpus/injection-test-faq.md`; the opt-in LIVE `npm run eval:rag` runner (`src/run-rag-eval.ts`, ephemeral temp store outside the repo, real embed + chat, never in `npm test`/CI) with dated evidence [`docs/rag-eval/2026-08-06.md`](rag-eval/2026-08-06.md); agentic retrieval deferred via [`docs/adr/0001-defer-agentic-retrieval.md`](adr/0001-defer-agentic-retrieval.md).
- **RATIFIED RAG-eval baseline thresholds (user-approved at Phase 8 closeout):** `meanCitationPrecision ≥ 0.70`, `groundednessPassRate ≥ 0.80`, `meanCitationRecall ≥ 0.80`, `injectionResistanceRate = 1.00`, `missingEvidenceAccuracy ≥ 0.80`; and the 8c `DEFAULT_MIN_EVIDENCE_SCORE = 0.5` is ratified with this same dated evidence. Met per [`docs/rag-eval/2026-08-06.md`](rag-eval/2026-08-06.md) (groundedness 1.000, citation precision 0.719 / recall 1.000 / F1 0.800, injection 1.000, missing-evidence 1.000, overall 0.750 ⇒ PASS). The precision bar 0.70 sits below the ~0.75 observed ceiling BY CONSTRUCTION (small topically-overlapping corpus + single-relevant-dominant labels + a stochastic model), not gate-gaming. Documented FOLLOW-UP: richer multi-source relevance labels and/or a larger, less-overlapping corpus to support a higher precision bar in a later phase.
- **Deliberate deferred work (future Phase 9 scope, not defects):** wiring the RAG/citation pipeline into the HTTP service (`npm run serve` / `src/core/sse.ts`) + the CLI (`npm run chat`) and the SSE event contract, and citation rendering in a UI, are Phase 9 — the pipeline is delivered as a pure/injected library and is not yet reachable from those surfaces.

**Approved design decisions (rationale for future readers)**

- **RAG shape:** a DETERMINISTIC TWO-STEP retrieve-then-generate pipeline (the roadmap's stated initial design). No agentic/iterative retrieval in this phase.
- **Citation trust model:** APP-ASSIGNED integer citation markers (e.g. `[1]`, `[2]`) placed in the prompt context; the APPLICATION maps each marker back to the retrieved chunk/document IDs and ACCEPTS a citation ONLY if it resolves to a chunk/document in the retrieved set. The model NEVER supplies raw IDs; any marker not in the retrieved set is dropped/flagged. This is the crux of exit criterion 1 (citations resolve to retrieved, authorized source IDs, never accepted solely from model text). Owner-scoped via the existing retriever `ownerId` authorization filter (`src/core/retriever.ts`).
- **Answer output:** STRUCTURED OUTPUT via `withStructuredOutput` + a Zod `GroundedAnswer` schema (fields: `answer`, `citations` [marker references], `insufficientEvidence` flag), reusing the Phase 6 structured-output infrastructure (`src/core/structured.ts`, `src/core/schemas.ts`) including the opus temperature-omit rule (`isTemperatureOmitModel`); default generation model `claude-sonnet-4-6`.
- **Prompt-injection defense:** retrieved content is treated as UNTRUSTED DATA, not instructions — a delimited context block plus a system-prompt guardrail; malicious-source-instruction resistance is validated by the 8d eval.
- **Wiring scope:** 8a–8d are delivered as a PURE/INJECTED library (mirroring the retriever/ingestion seams) — NOT wired into the HTTP service / CLI / SSE this phase (that is Phase 9). Everything is offline-testable end-to-end with a MOCKED model + a fake embedder; a LIVE opt-in eval is the only network path.
- **Eval evidence baseline:** the 8d implementer PROPOSES groundedness / citation precision-recall thresholds; the USER APPROVES them at Phase 8 closeout (same governance as the Phase 7 retrieval baseline) — they are inputs, never pre-marked "met".

**Increment plan (8a–8d)**

Phase 8 is delivered as ordered, independently verifiable sub-increments; each is delivered as implement → code-review → verify. One sub-increment is worked at a time.

- **8a — Grounded generation core.** A pure `answer()` pipeline — `retrieve()` → build a delimited context block where each retrieved chunk carries an APP-ASSIGNED citation marker mapped by app code to its chunkId/documentId → structured-output generation returning `{ answer, citations, insufficientEvidence }`. Injected model boundary (offline tests mock the model). Targets exit criterion 1 (foundation) and exit criterion 2.
- **8b — Trusted citation resolution.** App code resolves each model-emitted marker back to the retrieved, AUTHORIZED chunk/document IDs from the 8a context set; never accepts an ID from model text; drops/flags any marker not in the retrieved set; owner-scoped. Targets exit criterion 1.
- **8c — Insufficient-evidence behavior.** Deterministic "not enough grounded evidence" path when retrieval is empty/low-score or a claim can't be grounded; answers distinguish supported claims from insufficient evidence. Targets exit criterion 2.
- **8d — Evaluations.** Offline pure scorers + a dataset covering GROUNDEDNESS, CITATION precision/recall, PROMPT-INJECTION / malicious-source instructions, and MISSING-EVIDENCE; plus a LIVE opt-in eval producing dated evidence (`docs/rag-eval/<date>.md`) that is NEVER part of `npm test`/CI. Targets exit criterion 3.
- **Exit criterion 4** (any move to agentic retrieval requires comparative measurements + a separate ADR) is satisfied in this phase by an explicit DECISION RECORD deferring agentic retrieval — NO agentic implementation this phase.

**Scope boundary:** Phase 8 is grounded answers + trusted citations + evaluations ONLY, built on the deterministic two-step RAG design (no agentic retrieval). No web UI — that is Phase 9. Everything is delivered as a pure/injected library and is NOT wired into the HTTP service / CLI / SSE this phase.

### Phase 9 — Web Interface

**Status:** Complete

**Goal:** Deliver a secure browser experience over the stable service and event contracts.

**Key deliverables**

- Authenticated server-side credential handling; LiteLLM credentials never reach the browser.
- Stable typed Server-Sent Events (SSE) or equivalent event protocol.
- Thread creation/resumption and streaming, cancellation, retry, and reconnect behavior.
- Citation rendering, tool state, and structured artifacts.
- Browser end-to-end coverage, including unauthorized access attempts.

**Exit criteria**

- [x] The browser contains no provider credentials and all thread operations enforce authenticated ownership. (9c bearer-token auth → server-derived `ownerId` — the browser sends only the bearer token and NEVER an `ownerId`; `thread_id` is not identity — plus the HARD, TESTED zero-credentials-in-bundle guardrail `web/scripts/check-bundle-no-secrets.mjs` that builds + scans `web/dist` and fails on any provider secret (PASS, the concrete crit-1 artifact); 9b preserves the Phase-5 ownership model (`ownerId` from the token only, identical-404 not-owned == not-found, no existence leak); 9d E2E proves cross-owner isolation (identical 404, no data leak) + invalid-token generic auth failure with no secret leak end-to-end.)
- [x] Streaming/cancel/retry/reconnect behavior conforms to the versioned event contract. (9a versioned v2 contract — `SSE_PROTOCOL_VERSION` `2`, `init` carries `version`, backward-compatible with the v1 `init`/`token`/`done`/`error` shapes — plus 9c's typed v2 client that branches on `init.version` with partial-frame buffering, `AbortController` cancel, retry, and reconnect; 9d E2E exercises cancel→canceled, retry (no duplicate turn), and reconnect→error→recover.)
- [x] Citations, tool progress/errors, and artifacts render from typed trusted events. (9a typed citation/tool/artifact events — citations projected from the Phase 8 `TrustedCitation` (app-owned IDs resolved FROM the store, `ownerId` dropped from the wire), tool events model the Phase 6 agent message-stream shape, artifact is a typed envelope — emitted from the real server in 9b and rendered in 9c ONLY from the typed payloads (insufficient-evidence distinct; no fabricated citations); 9d E2E asserts the rendered citation fields EQUAL the scripted app-owned values (end-to-end v2 wire-compat proof).)
- [x] Browser end-to-end tests cover primary journeys, recovery paths, and unauthorized access. (9d Playwright suite — 8/8 green, deterministic, opt-in, SEPARATE from the offline `npm test` — over the REAL `createServer(deps)` (real auth/ownership/SSE-v2/redaction, only the model boundary faked via a scripted `answerTurn`, two-owner `AUTH_TOKENS`) serving the built SPA, covering primary journeys (auth → thread create/resume → grounded chat rendering trusted citations + artifact), recovery paths (cancel/retry/reconnect via route interception), and unauthorized access (invalid token → generic auth failure; cross-owner → identical 404).)

**Implementation evidence and notes**

- Phase 9 is complete (user-approved) with all four exit criteria met, each implemented → code-reviewed → verified across increments 9a–9d. The backend deterministic offline suite stands at 647 tests; the isolated `web/` project adds 31 web unit tests (web Vitest) plus 8 Playwright browser E2E specs (opt-in); `npm run typecheck` and `npm run build` pass. `web/` is a SEPARATE, isolated project (the root build/test never compile or run `web/`), and the browser E2E suite + the zero-credentials bundle scan are opt-in — NOT part of the offline `npm test`. No new model/eval baseline is introduced (the Phase 8 RAG baseline is already ratified; the browser E2E is the evidence).
- SSE v2 contract (9a): `src/core/sse.ts` extended to `SSE_PROTOCOL_VERSION` `2` with BACKWARD-COMPATIBLE typed citation/tool/artifact events (the v1 `init`/`token`/`done`/`error` shapes intact; clients branch on `init.version`); the citation payload is projected from the Phase 8 `TrustedCitation` (app-owned IDs; `ownerId` dropped from the wire), tool events model the Phase 6 agent message-stream, and the artifact is a typed envelope; the writer serializes only (the caller redacts).
- Server RAG wiring (9b): the Phase 8 `answerQuestion` is wired into the HTTP streaming chat handler via an injected `answerTurn` seam (`src/core/grounded-turn.ts`), emitting the v2 sequence `init→token→citation→artifact→done`; a pure tool-event translator (`src/core/agent-sse.ts`) with caller-side redaction — built and unit-tested but NOT yet wired to the live SSE endpoint (see deferred work below), so the v2 sequence above carries no live tool event; auth/ownership (`ownerId` from the bearer token only; identical-404; no existence leak), the idle watchdog, client-disconnect abort, and redaction are PRESERVED from Phase 5; a `SERVICE_GROUNDED` opt-in wires the live seam (default off).
- React+Vite browser SPA (9c): an isolated `web/` project — a typed v2 SSE client (fetch+POST+Bearer, branches on `init.version`, partial-frame buffering, `AbortController` cancel), bearer-token auth → server-derived `ownerId` (the client never sends `ownerId`; no provider creds in the browser), thread create/resume, streaming render, cancel/retry/reconnect, and citation/tool-progress/artifact/`evidenceStatus` rendering ONLY from typed trusted events; the ZERO-CREDENTIALS-IN-BUNDLE guardrail (`web/scripts/check-bundle-no-secrets.mjs`) builds + scans `web/dist` and fails on any provider secret (PASS — the concrete crit-1 artifact); 31 web unit tests.
- Playwright browser E2E (9d): `web/e2e` (opt-in, separate from the offline `npm test`) — a DETERMINISTIC harness runs the REAL `createServer(deps)` + real auth/ownership/SSE-v2/redaction with only the model boundary faked (scripted `answerTurn`; two-owner `AUTH_TOKENS`), serving the built SPA; 8 specs cover primary journeys (auth → thread create/resume → grounded chat rendering trusted citations + artifact, with the rendered citation fields asserted EQUAL to the scripted app-owned values = the end-to-end v2 wire-compat proof), recovery paths (cancel/retry/reconnect via route interception), and unauthorized access (invalid token → generic auth failure, no secret leak; cross-owner → identical 404, no data leak); `npm run e2e` / `e2e:install` are documented in `web/README.md`.
- **Deliberate deferred work (future scope, not defects):** (a) the LIVE react-AGENT tool-loop into the SSE endpoint — 9b wired RAG and built + tested the pure tool-event translator (`src/core/agent-sse.ts`), but the live agent hookup is deferred; (b) threading the `AbortSignal` into `answerQuestion`/the retriever/structured output so a mid-generation client disconnect CANCELS the billable call (currently the output is suppressed on disconnect but the in-flight call is not cancelled) — a Phase 10 follow-up; (c) full conversation-history replay on thread resume (resume currently re-fetches thread metadata + resets the transcript rather than replaying the full history). These are honest deferrals.

**Approved design decisions (rationale for future readers)**

- **Event contract:** EXTEND the existing typed SSE contract (`src/core/sse.ts`) to a VERSIONED v2 — bump `SSE_PROTOCOL_VERSION` to `2` and add typed events for CITATIONS (the Phase 8 `TrustedCitation` objects: app-owned IDs resolved FROM the store, never model text), TOOL STATE/ERRORS (the Phase 6 agent message-stream shape: `tool_calls` intention / `ToolMessage` result / `status: "error"`), and STRUCTURED ARTIFACTS — BACKWARD-COMPATIBLY: clients branch on `init.version`, and the v1 `init`/`token`/`done`/`error` shapes stay intact. Pure and offline-testable.
- **Backend:** KEEP the existing `node:http` `createServer(deps)` + SSE (no new web framework). Wire the Phase 8 RAG and the Phase 6 agent through the existing `StreamingChat` / `ServerDeps` seam (`src/core/server.ts`). Minimal new dependencies.
- **Frontend stack:** a REACT + VITE single-page app (React chosen over vanilla-TS for richer citation/tool/artifact rendering); a THIN client over the stable typed v2 contract.
- **Credentials:** server-side credential handling ONLY — LiteLLM credentials NEVER reach the browser. The React/Vite browser bundle must contain ZERO provider credentials (no `apiKey`/`baseURL`); this is a HARD, TESTED guardrail (a build/bundle check asserting no secret leaks into client assets), directly serving exit criterion 1.
- **Auth model:** UNCHANGED from Phase 5 — bearer token → `ownerId` (`src/core/auth.ts`); the browser sends the bearer token; the `ownerId` for every thread operation comes ONLY from the authenticated token, NEVER from `thread_id` or any client-supplied field. `thread_id` remains a server-issued conversation key, not identity or authorization.
- **End-to-end tooling:** PLAYWRIGHT browser end-to-end tests, run as an OPT-IN / CI-gated suite SEPARATE from the offline `npm test` (like the live evals/probes) so `npm test` stays fast and fully offline; covering primary journeys, recovery paths (cancel/retry/reconnect), and unauthorized-access attempts.
- **Quality gate:** the browser E2E suite is the exit-criterion-4 evidence; no new model/eval baseline is introduced (the Phase 8 RAG baseline is already ratified).

**Increment plan (9a–9d)**

Phase 9 is delivered as ordered, independently verifiable sub-increments; each is delivered as implement → code-review → verify. One sub-increment is worked at a time.

- **9a — Event contract v2 (typed).** Extend `src/core/sse.ts` to `SSE_PROTOCOL_VERSION` `2` with typed citation / tool-state / artifact events, backward-compatible (client branches on `init.version`; the v1 `init`/`token`/`done`/`error` shapes are preserved). Pure and offline-tested. Targets exit criterion 3 (foundation) and underpins exit criterion 2 (versioned contract).
- **9b — Server RAG/agent wiring.** Wire the Phase 8 `answerQuestion` (grounded answers + trusted citations + `evidenceStatus`) and the Phase 6 agent tool-loop into the server's `StreamingChat` seam, emitting v2 events; auth/ownership enforcement unchanged from Phase 5 (`ownerId` from the bearer token only). Offline-tested via injected deps + a mocked model. Targets exit criteria 1, 2, 3.
- **9c — React+Vite browser frontend.** A minimal, secure SPA — bearer-token auth, thread create/resume, streaming render, cancel/retry/reconnect against the v2 contract, and rendering of citations / tool progress+errors / structured artifacts from TYPED TRUSTED events; server-side credentials only (zero credentials in the browser bundle, enforced by a tested guardrail). Targets exit criteria 1, 2, 3.
- **9d — Browser E2E (Playwright, opt-in).** End-to-end coverage of primary journeys, recovery paths (cancel/retry/reconnect), and unauthorized-access attempts; NOT part of the offline `npm test` / CI-fast path. Targets exit criterion 4.

**Scope boundary:** Phase 9 is the browser experience over the stable service + versioned event contract ONLY. It wires the Phase 6 agent and the Phase 8 RAG/citations into the service/SSE + UI. Production hardening — rate limits, abuse controls, memory trimming/summarization, retention/privacy enforcement, routing/fallback policy, and load/failure validation — is Phase 10.

### Phase 10 — Production Hardening and Continuous Evaluation

**Status:** Planned

**Goal:** Establish production reliability, privacy, scalability, and continuous quality controls.

**Key deliverables**

- Memory trimming/summarization with regression evaluation.
- LiteLLM aliases, routing, and fallback policy, owned by LiteLLM where possible.
- Rate limits, abuse controls, retention/privacy enforcement, and user feedback capture.
- Offline and online evaluation loops.
- Load, concurrency, dependency-failure, cost, and latency validation with operational thresholds.

**Exit criteria**

- [ ] Memory controls remain within approved context/cost budgets without unacceptable quality loss.
- [ ] Routing/fallback behavior and failure modes are tested without duplicating provider routing in application code.
- [ ] Rate limits, privacy, retention, deletion, and feedback controls are auditable and exercised.
- [ ] Offline/online evaluations and production alerts have owners, thresholds, and response procedures.
- [ ] Load and failure tests meet approved reliability, cost, and latency targets.

**Implementation evidence and notes**

- These production controls and evaluations are not currently implemented.

## Dependencies and ordering principles

1. Verify proxy and chat-model capabilities before depending on tools, schemas, or structured outputs; assess embeddings separately against embedding aliases/endpoints.
2. Design trusted document identity and metadata before exposing citations.
3. Establish retrieval-only evaluation before tuning answer generation.
4. Establish durable persistence and thread ownership before building the web interface.
5. Stabilize and version the typed event contract before frontend integration.
6. Keep authentication/authorization context separate from `thread_id`; a thread identifier is not proof of identity or access.
7. Let LiteLLM own provider aliases, routing, and fallback where possible; avoid duplicating that policy in application code.
8. Do not activate Phase 7 or Phase 8 until an approved embedding alias and endpoint have been verified through the proxy. This gate is now MET: `gemini-embedding-001` is verified `Supported` at 768 dimensions ([`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md)); set `LITELLM_EMBED_MODEL` to a verified embedding alias for those phases.

## Phase-transition record

Add one concise row only after approval; do not use this table as a session log.

| Date | Phase | Approved transition | Approval reference | Exit criteria and verification evidence |
| --- | --- | --- | --- | --- |
| 2026-07-28 | Phase 4 | Active → Complete; Phase 5 becomes next proposed phase | User-approved Phase 4 closeout | All Phase 4 exit criteria ticked above; [`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md), [`docs/eval/2026-07-28.md`](eval/2026-07-28.md); 149 offline tests, `npm run typecheck` and `npm run build` pass |
| 2026-07-28 | Phase 5 | Planned → Active (user-approved); next proposed phase becomes Phase 6 | User-approved Phase 5 activation with approved design decisions | 5a in progress; no Phase 5 exit criteria met yet; increment plan and approved decisions recorded above |
| 2026-07-28 | Phase 5 | 5a Complete under active Phase 5; 5b becomes active | User-approved 5a delivery/review | Durable SQLite checkpointer behind `BaseCheckpointSaver` (survives restart), `createCheckpointer`/`getCheckpointer` + `CHECKPOINT_DB_PATH`; 154 offline tests incl. restart-survival + isolation; `npm run typecheck` and `npm run build` pass; reviewed. Phase 5 remains Active (top-level exit criteria unticked; closeout is 5d) |
| 2026-07-28 | Phase 5 | 5b Complete under active Phase 5; 5c becomes active | User-approved 5b delivery/review | Thread ownership model in `src/core/threads.ts` (`ThreadStore`/`createThreadStore`); server-issued UUIDs; owner-scoped list/get/delete with no existence leak; atomic hard delete of ownership row + checkpoint state in one transaction; shared SQLite connection via `getSqliteSaver()`; typed ownership gate via type-only `@types/better-sqlite3`; library-schema coupling guarded by version-pin + schema-introspection tests; 169 offline tests; `npm run typecheck` and `npm run build` pass; reviewed. Phase 5 remains Active (top-level exit criteria unticked; closeout is 5d) |
| 2026-07-28 | Phase 5 | 5c Complete under active Phase 5; 5d becomes active | User-approved 5c delivery/review | Authenticated `node:http` service (`src/core/auth.ts`/`src/core/sse.ts`/`src/core/server.ts`, live `npm run serve` via `src/run-server.ts`, no new HTTP framework dependency); bearer-token → `userId` with constant-time compare and generic 401; owner-scoped thread CRUD + SSE streaming chat enforcing the 5b ownership model; no existence leak (not-owned == not-found), chat 404 before any stream/checkpoint write; per-turn cancellation via `AbortController`; redacted client/log errors; versioned SSE contract (`SSE_PROTOCOL_VERSION=1`); injectable `createServer(deps)` for offline tests; `serve` entrypoint not imported by tests; 191 offline tests; `npm run typecheck` and `npm run build` pass; no repo db artifacts; security-reviewed (Ready, R2 fixed and re-reviewed). Phase 5 remains Active (top-level exit criteria unticked; closeout is 5d) |
| 2026-07-28 | Phase 5 | 5d Complete under active Phase 5; 5e added and becomes active | User-approved 5d delivery/review | HTTP `headersTimeout`/`requestTimeout` + per-turn SSE idle timeout (abort + single redacted `error` event, clean end, parser rejects 0/negatives, injectable via `deps.timeouts`); prototype-pollution 400 guard; `requestTimeout`-bounded 413; concurrency validated+documented (single synchronous connection; multi-instance out of scope); end-to-end restart durability through the service; atomic owner-scopeable `ThreadStore.pruneThreads` retention hook; docs (`AGENTS.md`/`README.md`/`.env.example`) aligned; 214 offline tests; `npm run typecheck` and `npm run build` pass; reviewed (Ready; N-1/N-2 fixed and re-reviewed). New increment 5e (schema versioning & migration) added Active to satisfy exit criterion (3)'s schema-migration clause. Phase 5 remains Active (top-level exit criteria unticked; closeout is after 5e) |
| 2026-07-28 | Phase 5 | Active → Complete (user-approved); Phase 6 becomes next proposed | User-approved Phase 5 closeout | All five Phase 5 exit criteria ticked above; 5a–5e Complete (5e app-owned schema versioning + forward-only migration runner in `src/core/migrations.ts`, dedicated `app_schema_migrations` marker, legacy-DB upgrade proven); durable SQLite persistence + owner-scoped ownership + authenticated SSE service; 225 offline tests, `npm run typecheck` and `npm run build` pass; reviewed. Phase 6 becomes the next proposed phase (not active until user-approved) |
| 2026-07-28 | Phase 6 | Planned → Active (user-approved); next proposed phase becomes Phase 7 | User-approved Phase 6 activation with approved design decisions | 6a in progress; no Phase 6 exit criteria met yet; increment plan (6a–6d) and approved decisions (Zod + `withStructuredOutput`; opus temperature-omit / sonnet default for structured output; pure read-only `days_until` + `split_budget` tools, no write tools; `createAgent` tool loop with the plain-chat graph retained; deterministic offline testing with a mocked tool-calling model) recorded above |
| 2026-07-28 | Phase 6 | 6a Complete under active Phase 6; 6b becomes active | User-approved 6a delivery/review | Typed Zod wedding-domain schemas in `src/core/schemas.ts` (plain LLM-facing `budgetPlanSchema`/`budgetCategorySchema` + `planningChecklistSchema`/`checklistItemSchema` with per-field constraints only) with the over-allocation business rule kept SEPARATE via a pure `validateBudgetAllocation` + strict `budgetPlanStrictSchema` for the 6b tool layer; structured-output helper `src/core/structured.ts` (`generateStructured`/`generateBudgetPlan`/`generatePlanningChecklist`) honoring the opus temperature-omit rule via a pure `decideStructuredModelOptions` (sonnet default), built through `createChatModel` (4b guard), with defense-in-depth `safeParse`, empty-input fast-fail, and redacted validation errors; `zod` direct dep `^4.4.3` (single resolved copy); 262 offline tests; `npm run typecheck` and `npm run build` pass; 4b/4c guards pass; reviewed (Ready; two Recommended items and two nitpicks fixed and re-reviewed). Phase 6 remains Active (top-level exit criteria unticked; closeout is 6d) |
| 2026-07-28 | Phase 6 | 6b Complete under active Phase 6; 6c becomes active | User-approved 6b delivery/review | Two SAFE, pure, deterministic, read-only tools in `src/core/tools.ts` with `tool()` wrappers + Zod input schemas — `days_until` (UTC calendar-day only, strict `YYYY-MM-DD` parsing, injectable `now`) and `split_budget` (default wedding split, custom percentages OR amounts, largest-remainder exact-sum rounding, reusing the 6a `validateBudgetAllocation`/`budgetPlanStrictSchema` over-allocation rule; under-allocation allowed); exported `weddingTools` registry for 6c; no agent/tool-loop or model construction (4b/4c guards intact); `src/core/schemas.ts` unmodified; 306 offline tests (`test/phase6-tools.test.ts`); `npm run typecheck` and `npm run build` pass; reviewed (Ready; Blocking finding B1 fixed and re-reviewed Ready, plus R1/R2/N2). Phase 6 remains Active (top-level exit criteria unticked; closeout is 6d) |
| 2026-07-28 | Phase 6 | 6c Complete under active Phase 6; 6d becomes active | User-approved 6c delivery/review | Wedding-planning tool-loop agent in `src/core/agent.ts` (`createWeddingAgent`/`runWeddingAgent`, `DEFAULT_AGENT_MODEL=claude-sonnet-4-6`) wiring the 6b `weddingTools` into LangGraph's prebuilt `createReactAgent({ llm, tools, prompt })` with the Aria persona, building the model via `createChatModel` (4b guard) and honoring the opus temperature-omit rule via the shared `isTemperatureOmitModel` predicate; plain-chat graph (`chain.ts`) untouched, no HTTP/CLI wiring (deferred to 6d); headline exit-criterion "days until 2026-12-12 + split $30k budget" proven OFFLINE with a scripted tool-calling fake (`test/helpers/fake-model.ts`) + REAL react loop/`ToolNode`/tools (both tools called, budget sums EXACTLY to 30000), plus single/no/tool-error paths and the opus-temp decision (`test/phase6-agent.test.ts`, 11 tests); 317 offline tests; `npm run typecheck` and `npm run build` pass; 4b/4c guards pass; no repo db artifacts; reviewed (Ready; no blocking; one Recommended R1 + a nitpick addressed as doc-only comments). Known future-migration item: `createReactAgent` is symbol-level `@deprecated` in `langgraph` 1.4.7 — migrating to the `langchain` `createAgent` is a deliberate future decision (prebuilt import intentionally kept per the accepted no-meta-package decision). Phase 6 remains Active (top-level exit criteria unticked; closeout is 6d) |
| 2026-08-05 | Phase 6 | Active → Complete (user-approved; criterion-3 interpretation endorsed); Phase 7 becomes next proposed | User-approved Phase 6 closeout | All four Phase 6 exit criteria ticked above; 6a–6d Complete (6d bounded tool timeout `src/core/tool-runtime.ts`, refusal/malformed/provider handling in `src/core/structured.ts` incl. N1 empty-object hardening, only-permitted + tool-state/error agent message-contract tests, opt-in per-alias live contract probe `src/probe-contracts.ts` / `npm run test:contracts`); criterion 3 satisfied at the agent typed message-stream level (`tool_calls` / `ToolMessage` `status`) — the user-endorsed reading, with agent→SSE/CLI wiring deliberately deferred; criterion 4 evidence [`docs/contracts/2026-08-05.md`](contracts/2026-08-05.md) (opus + sonnet both `Supported`, opus with temperature omitted); 357 offline tests, `npm run typecheck` and `npm run build` pass; reviewed. No phase active; Phase 7 becomes the next proposed phase (not active until user-approved) |
| 2026-08-05 | Phase 7 | Planned → Active (user-approved) | User-approved Phase 7 activation with approved design decisions and increment plan | No Phase 7 exit criteria met yet; increment plan 7a–7e and approved decisions (sqlite-vec on better-sqlite3; separate `KNOWLEDGE_DB_PATH` DB with Phase-5 versioned migrations; `gemini-embedding-001` @ 768 dims via `LITELLM_EMBED_MODEL`, gate met per [`docs/capabilities/2026-07-28.md`](capabilities/2026-07-28.md); app-owned content-hash IDs + deterministic chunking; trusted authorization/citation-ready metadata; offline fake-embedder tests + opt-in live retrieval eval) recorded above; Phase 8 becomes next proposed |
| 2026-08-06 | Phase 7 | Active → Complete (user-approved; baseline thresholds approved; criterion-4 native-vs-truncated interpretation accepted) | User-approved Phase 7 closeout | All five Phase 7 exit criteria ticked above; 7a–7e Complete; approved retrieval baseline recall@k≥0.80/precision@k≥0.18/MRR≥0.70/nDCG@k≥0.75 met per [`docs/retrieval/2026-08-06.md`](retrieval/2026-08-06.md); crit-4 native-vs-truncated interpretation with regenerated [`docs/embeddings/2026-08-06.md`](embeddings/2026-08-06.md); 498 offline tests, `npm run typecheck` and `npm run build` pass; reviewed. No phase active; Phase 8 becomes next proposed (not active until user-approved) |
| 2026-08-06 | Phase 8 | Planned → Active (user-approved) | User-approved Phase 8 activation with approved design decisions and increment plan | No Phase 8 exit criteria met yet; increment plan 8a–8d and approved decisions (deterministic two-step retrieve-then-generate; app-assigned citation markers resolved by app code to retrieved/authorized IDs, never from model text; Zod `GroundedAnswer` via `withStructuredOutput` reusing Phase 6 structured output incl. opus temp-omit, default `claude-sonnet-4-6`; retrieved content treated as untrusted data w/ prompt-injection guardrail; PURE/injected library not wired to service/CLI/SSE this phase; groundedness/citation eval thresholds PROPOSED pending user approval at closeout; agentic retrieval deferred via ADR) recorded above; Phase 9 becomes next proposed |
| 2026-08-06 | Phase 8 | Active → Complete (user-approved; RAG-eval baseline ratified — meanCitationPrecision 0.70, others as proposed; DEFAULT_MIN_EVIDENCE_SCORE 0.5) | User-approved Phase 8 closeout | All four Phase 8 exit criteria ticked above; 8a–8d Complete; grounded two-step RAG with app-owned trusted citations (crit 1), evidenceStatus supported-vs-insufficient reconciliation (crit 2), deterministic RAG eval across groundedness/citation P-R/injection/missing-evidence with ratified baseline met per [`docs/rag-eval/2026-08-06.md`](rag-eval/2026-08-06.md) (crit 3), agentic retrieval deferred via [`docs/adr/0001-defer-agentic-retrieval.md`](adr/0001-defer-agentic-retrieval.md) (crit 4); 602 offline tests, `npm run typecheck` and `npm run build` pass; reviewed. No phase active; Phase 9 becomes next proposed (not active until user-approved) |
| 2026-08-06 | Phase 9 | Planned → Active (user-approved) | User-approved Phase 9 activation with approved design decisions and increment plan | No Phase 9 exit criteria met yet; increment plan 9a–9d and approved decisions (extend typed SSE to versioned v2 with citation/tool-state/artifact events, backward-compatible; keep `node:http` `createServer(deps)`+SSE, wire RAG/agent via `StreamingChat`; React+Vite thin client; ZERO provider creds in the browser bundle as a tested guardrail; Phase-5 auth unchanged — `ownerId` from bearer token only, `thread_id` is not identity; Playwright browser E2E opt-in, separate from offline `npm test`) recorded above; Phase 10 becomes next proposed |
| 2026-08-06 | Phase 9 | Active → Complete (user-approved) | User-approved Phase 9 closeout | All four Phase 9 exit criteria ticked above; 9a–9d Complete (9a SSE v2 contract; 9b server RAG wiring emitting v2 events, Phase-5 auth/ownership/redaction preserved; 9c isolated React+Vite SPA + tested zero-creds bundle guardrail `web/scripts/check-bundle-no-secrets.mjs`; 9d deterministic opt-in Playwright browser E2E 8/8 across primary/recovery/unauthorized with a v2 wire-compat proof); backend 647 offline tests + web 31 unit + 8 E2E; `npm run typecheck`/`npm run build`/bundle-scan pass; reviewed. Deferred to later phases: live agent-tool-loop into the SSE endpoint, `AbortSignal` propagation into generation, full history replay on resume. No phase active; Phase 10 becomes next proposed (not active until user-approved) |
